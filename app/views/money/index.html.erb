<p id="notice"><%= notice %></p>

<h1>Enter your information:</h1>

<table>
  <thead>
    <tr>
      <th>balance</th><br>
      <th>Deposits</th><br>
      <th>Withdrawals</th><br>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @money.each do |money| %>
      <tr>
        <td><%= money.balance %></td>
        <td><%= money.deposits %></td>
        <td><%= money.withdrawals %></td>
        <td><%= link_to 'Show', money %></td>
        <td><%= link_to 'Edit', edit_money_path(money) %></td>
        <td><%= link_to 'Destroy', money, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<p> Given your recent balance, currently you have:</p>
<p><%= @totalcash %></p>

<p>Your total transactions so far:</p>
<p><%= @moneycount %></p>
<%= link_to 'New Transaction', new_money_path %>
